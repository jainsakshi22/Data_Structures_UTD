/** @author Sakshi Jain
 */

** Steps to compile and run the program:

* To run through command line, follow these steps:
1. Copy the sxj180002 (Package) folder in a directory
2. Open terminal and go to the package folder
3. First Compile Filename using the command "javac packageName/Filename.java"
4. Compile Filename using the command "javac packageName/Filename.java"
5. To execute the Filename main method, use command "java sxj180002.Filename"
